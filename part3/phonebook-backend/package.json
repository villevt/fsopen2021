{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && cd ../phonebook-frontend && npm run build && cp -r build ../notes-backend",
    "deploy": "npm run build && git add . && git commit -m deploy && cd ../../ git && git subtree push -P part3/phonebook-backend heroku master"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
